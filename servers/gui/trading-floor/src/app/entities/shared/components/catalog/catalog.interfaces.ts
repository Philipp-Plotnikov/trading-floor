export namespace ICatalog {
	export interface CatalogData {
		id: string;
		imgUrl: string;
		description: string;
		price: number;
		mark: number;
	}
}

/*
 * Catalogs
 * [${catalogName}]: "[{}, {}, ...]";
 * */
